fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
pytest>=8
# Real black-box backend (MODEL_BACKEND=openai). Lightweight.
openai>=1.0
# Optional local open-model backend (MODEL_BACKEND=hf). Not needed for the PoC.
# transformers>=4.44
# torch
# accelerate
